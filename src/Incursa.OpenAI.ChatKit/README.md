# Incursa.OpenAI.ChatKit

Core ChatKit runtime for .NET. This package contains the exact wire models, request routing, widget streaming utilities, in-memory store, and agent helper surface used by the ASP.NET Core host layer.
