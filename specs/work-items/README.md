---
workbench:
  type: work-item
  workItems: []
  codeRefs: []
  pathHistory: []
  path: /specs/work-items/README.md
---

# Work Items

This directory is reserved for canonical SpecTrace `WI-*` artifacts when ChatKit delivery slices need tracked implementation work.

The repository does not yet maintain a ChatKit work-item corpus. Add package-scoped `WI-*` artifacts here when new requirement slices move from authoring into explicitly tracked implementation.
